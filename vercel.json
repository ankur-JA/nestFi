{
  "buildCommand": "cd packages/nextjs && yarn build",
  "outputDirectory": "packages/nextjs/.next",
  "installCommand": "corepack enable && corepack prepare yarn@3.2.3 --activate && yarn install",
  "framework": "nextjs",
  "rootDirectory": "packages/nextjs",
  "ignoreCommand": "test -z \"$(git diff --name-only HEAD^ HEAD packages/nextjs/ 2>/dev/null)\""
}
