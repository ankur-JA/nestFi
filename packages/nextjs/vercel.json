{
  "buildCommand": "cd ../.. && corepack enable && corepack prepare pnpm@latest --activate && cd packages/nextjs && pnpm run build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@latest --activate && pnpm install",
  "framework": "nextjs",
  "ignoreCommand": "test -z \"$(git diff --name-only HEAD^ HEAD .)\""
}
