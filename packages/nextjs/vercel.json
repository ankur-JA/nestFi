{
  "buildCommand": "cd ../.. && corepack enable && corepack prepare yarn@3.2.3 --activate && cd packages/nextjs && corepack yarn build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && corepack enable && corepack prepare yarn@3.2.3 --activate && cd packages/nextjs && corepack yarn install",
  "framework": "nextjs",
  "ignoreCommand": "test -z \"$(git diff --name-only HEAD^ HEAD .)\""
}
